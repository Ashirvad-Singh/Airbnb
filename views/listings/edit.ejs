<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Listing</title>
</head>
<body>
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">
        <!-- Method override for PUT -->
        <input type="hidden" name="_method" value="PUT">

        <label for="title">Title</label>
        <input id="title" name="listing[title]" value="<%= listing.title %>" placeholder="Title" required>
        <br><br>

        <label for="description">Description</label>
        <textarea id="description" name="listing[description]" placeholder="Description" required><%= listing.description %></textarea>
        <br><br>

        <label for="image">Image URL</label>
        <input id="image" type="url" name="listing[image]" value="<%= listing.image %>" placeholder="Image URL">
        <br><br>

        <label for="price">Price</label>
        <input id="price" type="number" name="listing[price]" value="<%= listing.price %>" placeholder="Price" min="0" required>
        <br><br>

        <label for="location">Location</label>
        <input id="location" type="text" name="listing[location]" value="<%= listing.location %>" placeholder="Location">
        <br><br>

        <label for="country">Country</label>
        <input id="country" type="text" name="listing[country]" value="<%= listing.country %>" placeholder="Country">
        <br><br>

        <button type="submit">Edit Listing</button>
    </form>
</body>
</html>
